.col-lg-12
  %h1= t("auth.password_reset.title")

  = simple_form_for @user, :url => password_reset_path(@user), html: {method: :put} do |f|
    = f.input :email, label: false, placeholder: t("auth.placeholders.email"), input_html: { class: "form-control", readonly: "readonly" }
    = f.input :password, label: false, placeholder: t("auth.placeholders.password"), input_html: { class: "form-control" }
    = f.input :password_confirmation, label: false, placeholder: t("auth.placeholders.password_confirmation"), input_html: { class: "form-control" }
    = hidden_field_tag :token, @token

    .form-actions
      = f.submit t("auth.password_reset.reset_action"), class: "btn btn-primary"